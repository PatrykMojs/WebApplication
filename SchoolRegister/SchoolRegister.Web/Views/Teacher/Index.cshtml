@model IEnumerable<TeacherVm>

@{
    ViewData["Title"] = "Lista Nauczycieli";
}

<h2>Lista Nauczycieli</h2>

<table class="table">
    <thead>
        <tr>
            <th>Imię</th>
            <th>Nazwisko</th>
            <th>Przedmioty</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var teacher in Model)
        {
            <tr>
                <td>@teacher.FirstName</td>
                <td>@teacher.LastName</td>
                <td>@teacher.SubjectNames</td>
            </tr>
        }
    </tbody>
</table>
